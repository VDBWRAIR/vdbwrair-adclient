#!/bin/bash
#
# Simple script to join computer to the domain
# It has only one parameter which is the username to join
# the computer as
#
net ads join -S <%= @smb_adservers[0] %> -U "$1" createcomputer="<%= @computerou %>" osName="<%= @os['name'] %>" osVer="<%= @os['release']['full'] %>"
